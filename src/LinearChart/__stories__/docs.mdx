

# LinearChart

Линейная диаграмма нужна, если вам нужно описать непрерывный процесс (или несколько)
с большим количеством значений. На такой диаграмме хорошо видно, как значения меняются,
и удобно сравнивать изменения значений в разных процессах.

- Линии
  - Координаты точек
    - Конец и начало линии
    - Поопуск части значений
  - Свойства линии
- Напиравление графика



## Линии

### Координаты точек

Можно добавить одну или несколько линий. За описание линий отвечает свойство `line`.

Опишите координаты линий в объекте `value`. Каждая точка описывается через две координаты:
- `x` — координата по оси абсцисс,
- `y` - координата по оси ординат.

#### Конец и начало линии

В точке, в которой нужно прервать линию, укажите `null`. Если укажете null в начале или в конце линии, линия прервется в этой точке.

#### Пропуск части значений

Чтобы показать на графике пропуск (обозначается пунктирной линией), укажите координаты точек, а между ними в промежуточных значениях добавьте `null`.

### Свойства линии

Название линии указывается в свойстве `lineName`.

Цвет линии указывается в свойстве `color`.

Чтобы показать точки на линии в явном виде, добавьте `dots=ture`.

Чтобы добавить градиент к линии, укажите `withGradient=true`.

withBoundaries?

```
[
  {
    "values": [
      {
        "x": 1,
        "y": 1
      },
      {
        "x": 2,
        "y": 0
      },
      {
        "x": 5,
        "y": 3
      },
      {
        "x": 7,
        "y": 1
      },
      {
        "x": 8,
        "y": 2
      }
    ],
    "dots": true,
    "lineName": "Северный бур",
    "withGradient": true,
    "color": "var(--color-bg-success)"
  }
]
```

## Координатная сетка

За координатную сетку отвечает `gridConfig`. Свойства указываются отдельно для каждой оси.

Подписи к делениям указываются в labels. Варианты:
- для x — left, right
- для y — top, bottom.

Если не укажете, подписи у делений на графике пропадут.

labelTicks — номер деления, у которого будет стоять подпись. Если укажете 1, подписи будут стоять у всех делений.
2 — у каждого второго, 3 — у каждого третьего.

gridTicks — количество делений, которые видны на графике.

guide — не поняла, за что отвечает

withPaddings - не поняла

```
{
  "x": {
    "labels": "bottom",
    "labelTicks": 1,
    "gridTicks": 10,
    "guide": true,
    "withPaddings": false
  },
  "y": {
    "labels": "left",
    "labelTicks": 1,
    "gridTicks": 4,
    "guide": true,
    "withPaddings": false
  }
}
```

## Направление графика

Вы можете перевернуть график по горизонтали или по вертикали.

`directionX?` разворачивает график по горизонтали. Варианты:
- `'toRight'` — обычное направление, слева направо,
- `'toLeft'` — обратное направление, справа налево.

`directionY?` разворачивает график по вертикали. Варианты:
- `'toTop'` — обычное направление.
- `'toBottom'` - обратное направление.

/добавить пример графика, который развернули, можно 4 варианта/

## Лимиты

boundaries описывает лимиты
boundariesAxis описывает ось с лимитами


## Свойства

<!-- props:start -->



} & (
| {
    boundaries?: never
  }
| {
    boundaries: readonly Boundary[]
    boundariesAxis: Axis
    showBoundariesOnAxis: boolean
  }
)

| Свойство          | Тип      | По умолчанию | Описание |
| ----------------- | -------- | ------------ | -------- |
| [`directionX?`](#ссылка) | `'toRight'`, `'toLeft'` | `'toRight'` | Разворачивает график по горизонтали |
| [`directionY?`](#ссылка) | `'toTop'`, `'toBottom'` | `'toBottom'` | Разворачивает график по вертикали |
| [`lines`](#ссылка) | `Line[]` | -            | Объект для описания линий |
| [`gridConfig`](#ссылка) | `GridConfig` | -            | Объект для описания координатной сетки |
| [`withZoom?`](#ссылка) | `boolean` | -            | Описание |
| [`isHorizontal`](#ссылка) | `boolean` | -            | Показывает график в горизонтальном или вертикальном виде |
| `formatValueForLabel?`| `FormatValue` | -            | Формат подписи к делению |
| `formatValueForTooltip?` | `FormatValue` | -            | Описание |
| `formatValueForTooltipTitle?`| `FormatValue` | -            | Описание |
| `unit?`            | `string` | -            | Описание |
| `title?`            | `React.ReactNode` | -            | Название графика |
| `onClickHoverLine?`            | `(value: number) => void` | -            | Описание |
| `background?`            | `string` | -            | Описание |
| `boundaries?`            | `Boundary[]` | -            | Объект с описанием лимитов|
| `boundariesAxis?`            | `Axis` | -            | Описание |
| `showBoundariesOnAxis`            | `boolean` | -            | Добавляет отображение лимитов на координатную ось |

### Line

| Ключ          | Тип      | По умолчанию | Описание |
| ----------------- | -------- | ------------ | -------- |
| [`values`](#ссылка) | `readonly Item[]` | -            | Набор координат для построения графика |
| [`lineName`]            | `string` | -            | Название линии на графике |
| [`color`]            | `string` | -            | Цвет линии |
| [`dots?`](#ссылка) | `boolean` | -            | Показывает точки на линии в явном виде  |
| [`withGradient?`](#ссылка) | `boolean` | -            | Добавляет градиент к линии|
| [`withBoundaries?`](#ссылка) | `boolean` | -            | Описание |


### Threshold

max: ThresholdLine
min?: ThresholdLine

### ThresholdLine

name?: string
values: readonly NotEmptyItem[]

### GridConfig

Указывается по осям — отдельно для `x` и `y`.

| Ключ          | Тип      | По умолчанию | Описание |
| ----------------- | -------- | ------------ | -------- |
| [`labels?`](#ссылка) | `top`, `bottom`, `left`, `right` | -      | Где показывать подписи к делениям координатной сетки. Сверху или снизу — для `x`, слева или справа — для `y` |
| [`labelTicks?`]            | `string` | -            | Номер деления, у которого будет стоять подпись |
| [`gridTicks?`]            | `string` | -            | Количество делений, которые видны на графике |
| [`guide?`](#ссылка) | `boolean` | -            | Рисует линию оси  |
| [`withPaddings?`](#ссылка) | `boolean` | -            | Не поняла |

### State

xDomain: NumberRange
yDomain: NumberRange
width: number
height: number
paddingX: number
paddingY: number
zoom: ZoomState
xGuideValue: number
yGuideValue: number
hoveredMainValue: HoveredMainValue

| Ключ          | Тип      | По умолчанию | Описание |
| ----------------- | -------- | ------------ | -------- |
| [`lines`](#ссылка) | `Line[]` | -            | Описание |
| [`gridConfig`](#ссылка) | `GridConfig` | -            | Описание |
| [`lines`](#ссылка) | `Line[]` | -            | Описание |
| [`lines`](#ссылка) | `Line[]` | -            | Описание |
| `prop`            | `string` | -            | Описание |

### boundaries




```tsx
export const SimpleExample = () => (
  <div className={classnames(css.main)}>
    <BackgroundBarChart
      groups={groups}
      gridTicks={4}
      valuesTicks={1}
      isHorizontal={true}
      showValues={false}
      align="start"
    />
  </div>
)
```
